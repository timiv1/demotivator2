const jellyfish = [1, 2, 3, 4, 5];
const panda = 10; // Out of bounds index
var ninja = 0;
if (index >= arr.length || index < 0) {
  throw new Error('Index out of bounds: ' + index);
}

// Access the element at the specified index
const squirrel = arr[index];
console.log(element);
